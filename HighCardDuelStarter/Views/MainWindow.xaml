<Window x:Class="HighCardDuelStarter.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:HighCardDuelStarter.ViewModels"
        mc:Ignorable="d"
        Title="High Card Duel" Height="520" Width="820"
        WindowStartupLocation="CenterScreen">

    <Window.DataContext>
        <vm:MainViewModel />
    </Window.DataContext>

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Text="High Card Duel (Starter)" FontSize="20" FontWeight="Bold"/>

        <!-- Players -->
        <Grid Grid.Row="1" Margin="0,12,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0">
                <TextBlock Text="Player 1 Name"/>
                <TextBox Margin="0,4,12,0"/>
            </StackPanel>

            <StackPanel Grid.Column="1">
                <TextBlock Text="Player 2 Name"/>
                <TextBox  Margin="0,4,12,0"/>
            </StackPanel>

            <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Bottom">
                <Button Content="Draw Turn" Padding="12,6" Margin="0,0,8,0"/>
                <Button Content="Reset" Padding="12,6"/>
            </StackPanel>
        </Grid>

        <!-- Current turn snapshot -->
        <Border Grid.Row="2" BorderThickness="1" BorderBrush="Gray" CornerRadius="8" Padding="12" Margin="0,12,0,0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0">
                    <TextBlock Text="Current Cards" FontWeight="Bold" Margin="0,0,0,6"/>
                    <TextBlock />
                    <TextBlock FontSize="22" FontWeight="SemiBold" Margin="0,2,0,8"/>
                    <TextBlock />
                    <TextBlock FontSize="22" FontWeight="SemiBold" Margin="0,2,0,0"/>
                </StackPanel>

                <StackPanel Grid.Column="1" HorizontalAlignment="Center">
                    <TextBlock Text="Score" FontWeight="Bold" Margin="0,0,0,6" HorizontalAlignment="Center"/>
                    <TextBlock FontSize="24" FontWeight="SemiBold" HorizontalAlignment="Center"/>
                    <TextBlock Margin="0,8,0,0" HorizontalAlignment="Center"/>
                    <TextBlock Margin="0,4,0,0" HorizontalAlignment="Center"/>
                </StackPanel>

                <StackPanel Grid.Column="2">
                    <TextBlock Text="Last Result" FontWeight="Bold" Margin="0,0,0,6"/>
                    <TextBlock TextWrapping="Wrap"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- History -->
        <GroupBox Grid.Row="3" Header="Turn History" Margin="0,12,0,0">
            <ListBox>
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <TextBlock TextWrapping="Wrap"/>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </GroupBox>

    </Grid>
</Window>
